[package]
name = "graphql-orm-router-protocol"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/Dastari/graphql-orm"
description = "Project-neutral GraphQL subgraph and router interoperability declarations"
publish = false

[package.metadata.distribution]
method = "git-full-revision"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
